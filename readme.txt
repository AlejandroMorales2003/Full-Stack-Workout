To run the backend code:
Run the back end in a separate terminal by typing npm start. This will access the mongodb database and start the express server.

To run the front end:
Make sure the back backend is running correctley and is connected to the mongodb and localhost:3000. Then in separate terminal type npm start
from the front_end folder. Type localhost:3000 into the web browser.

